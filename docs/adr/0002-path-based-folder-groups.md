# Model folder groups as relative paths

RepoMirror stores each Folder Group as a relative path beneath one Root Directory rather than as nested group records. The interface renders those path segments as a tree, while root-level Sync Items remain directly under their Root Directory; this supports arbitrary nesting without a recursive configuration model.
