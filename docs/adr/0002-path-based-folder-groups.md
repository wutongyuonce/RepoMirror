# Model folder groups as relative paths

RepoMirror will store a Folder Group as one relative path beneath the Default Root Directory rather than as nested group records. The interface renders those path segments as a tree, while root-level Sync Items remain in a fixed root container; this supports arbitrary nesting without a recursive configuration model.
