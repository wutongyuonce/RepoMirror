{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for RepoMirror's main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
